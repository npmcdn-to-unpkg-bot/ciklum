import { bootstrap } from '@angular/platform-browser-dynamic';
import { AppLottoComponent } from './app.lotto.component';
import { HTTP_PROVIDERS } from '@angular/http';

bootstrap(AppLottoComponent, [ HTTP_PROVIDERS ]);

